server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/myntra_customers_db
    username: BHANU
    password: BHANU
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  thymeleaf:
    enabled: true
    cache: false



logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org:
      springframework:
        security: DEBUG

